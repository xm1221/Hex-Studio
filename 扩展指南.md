# Hex-Studio 扩展指南

## 目录

- [项目架构速览](#项目架构速览)
- [一、新增 Pattern（图案）](#一新增-pattern图案)
  - [1.1 Pattern 结构体字段说明](#11-pattern-结构体字段说明)
  - [1.2 Action 函数签名与辅助工具](#12-action-函数签名与辅助工具)
  - [1.3 可用的类型 Getter](#13-可用的类型-getter)
  - [1.4 完整示例：新增一个数学 Pattern](#14-完整示例新增一个数学-pattern)
  - [1.5 新增 Great Spell（长签名图案）](#15-新增-great-spell长签名图案)
  - [1.6 新增 Meta Pattern（UI 操作图案）](#16-新增-meta-patternui-操作图案)
- [二、新增 Iota 类型](#二新增-iota-类型)
  - [2.1 当前 Iota 类型一览](#21-当前-iota-类型一览)
  - [2.2 必须修改的文件完整清单](#22-必须修改的文件完整清单)
  - [2.3 逐步操作指南](#23-逐步操作指南)
  - [2.4 新增 Iota 后要检查的检查清单](#24-新增-iota-后要检查的检查清单)
- [三、图案签名系统说明](#三图案签名系统说明)
- [四、outputOptions 与 selectedOutput 机制](#四outputoptions-与-selectedoutput-机制)

---

## 项目架构速览

```
Hex-Studio-master/
├── index.html                      # 入口 HTML
├── elm.json                        # Elm 项目配置
├── src/                            # Elm 源码
│   ├── Main.elm                    # 应用入口 (TEA: Model/Update/View)
│   ├── Logic/App/
│   │   ├── Types.elm               # ★ Iota、Pattern、Direction 等核心类型
│   │   ├── Model.elm               # ★ 全局状态模型
│   │   ├── Msg.elm                 # ★ 所有消息类型 (~70 种)
│   │   ├── Grid.elm                # 六边形网格坐标计算
│   │   ├── Patterns/
│   │   │   ├── PatternRegistry.elm # ★★★ 图案注册表（核心扩展点）
│   │   │   ├── OperatorUtils.elm   # ★★★ Action 辅助函数（核心扩展点）
│   │   │   ├── MetaActions.elm     # 元操作实现
│   │   │   ├── Math.elm            # 数学运算图案
│   │   │   ├── Spells.elm          # 法术图案
│   │   │   ├── GreatSpells.elm     # 高级法术图案
│   │   │   ├── Stack.elm           # 栈操作图案
│   │   │   ├── Lists.elm           # 列表操作图案
│   │   │   ├── Selectors.elm       # 选择器图案
│   │   │   ├── ReadWrite.elm       # 读写图案
│   │   │   ├── Misc.elm            # 杂项图案
│   │   │   └── Circles.elm         # 圆形图案（未实现）
│   │   ├── Stack/EvalStack.elm     # 栈式虚拟机执行引擎
│   │   ├── PatternList/PatternArray.elm  # Pattern 数组操作
│   │   ├── ImportExport/
│   │   │   ├── ImportParser.elm           # 文本导入解析
│   │   │   └── ImportExportProject/ImportExportProjectV1.elm  # 项目序列化
│   │   ├── Macros/UpdateMacroReferences.elm  # 宏引用更新
│   │   └── Utils/
│   │       ├── GetIotaValue.elm    # Iota → 字符串转换（UI显示）
│   │       ├── GetAngleSignature.elm  # 路径 → 签名转换
│   │       ├── DirectionMap.elm    # 方向 → 坐标映射
│   │       └── LetterMap.elm       # 字母 → 方向映射
│   ├── Components/App/             # UI 组件层
│   ├── Ports/                      # Elm ↔ JS 互操作
│   └── Settings/Theme.elm          # 颜色主题
└── gen/                            # 编译产物 (JS, CSS, WASM)
```

---

## 一、新增 Pattern（图案）

**难度：★☆☆ 很容易 | 侵入性：仅需修改 1 个文件 | 无需理解执行引擎内部细节**

### 1.1 Pattern 结构体字段说明

`Pattern` 定义在 `src/Logic/App/Types.elm`（第34-45行）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `signature` | `String` | 六边形路径签名，由字母 `a/w/d/e/q` 组成，如 `"waaw"`、`"qaq"` |
| `startDirection` | `Direction` | 起始方向，可选值：`Northeast` / `Northwest` / `East` / `West` / `Southeast` / `Southwest` |
| `action` | `Array Iota -> CastingContext -> ActionResult` | **核心函数**：从栈取输入、产出新栈、可能修改上下文 |
| `metaAction` | `MetaActionMsg` | 元操作：`None` / `ClearPatterns` / `Reset` / `Backspace` / `Wrap` |
| `displayName` | `String` | UI 中显示的名称，如 `"Additive Distillation"` |
| `internalName` | `String` | 内部标识符，如 `"add"`、`"swap"`，用于文本导入识别 |
| `color` | `String` | 图案颜色（CSS 颜色值），通常用 `accent1` (`"#BAC5E2"`) |
| `outputOptions` | `List IotaType` | 可能的输出类型列表，用于处理输出不确定的图案（如 `[VectorType, NullType]`） |
| `selectedOutput` | `Maybe (IotaType, Iota)` | 预选的默认输出值，如 `Just (NumberType, Number 0)` |
| `active` | `Bool` | 是否激活（通常为 `True`） |

### 1.2 Action 函数签名与辅助工具

每个 Pattern 的 `action` 函数签名为：

```elm
Array Iota -> CastingContext -> ActionResult
```

其中 `ActionResult` 定义为：
```elm
type alias ActionResult =
    { stack : Array Iota       -- 新栈（执行后的栈状态）
    , ctx : CastingContext     -- 可能被修改的施法上下文
    , success : Bool           -- 是否成功
    }
```

`OperatorUtils.elm` 提供了以下辅助函数，**强烈建议使用它们而非手动操作栈**：

#### Action 系列（消耗输入并产出新值）

| 函数 | 消耗栈顶元素数 | 用法 |
|------|:---:|------|
| `actionNoInput stack ctx action` | 0 | 无输入操作，`action` 接收 `ctx`，返回 `(newValues, newCtx)` |
| `action1Input stack ctx getter action` | 1 | 单输入，`action` 接收 `(iota, ctx)`，返回 `(newValues, newCtx)` |
| `action2Inputs stack ctx getter1 getter2 action` | 2 | 双输入，`action` 接收 `(iota1, iota2, ctx)` |
| `action3Inputs stack ctx getter1 getter2 getter3 action` | 3 | 三输入，`action` 接收 `(iota1, iota2, iota3, ctx)` |

#### Spell 系列（消耗输入但只修改世界/上下文，不产出新栈值）

| 函数 | 消耗栈顶元素数 | 说明 |
|------|:---:|------|
| `spellNoInput stack ctx` | 0 | 无输入法术（如 Summon Rain） |
| `spell1Input stack ctx getter` | 1 | 单输入法术（如 Break Block） |
| `spell2Inputs stack ctx getter1 getter2` | 2 | 双输入法术（如 Explosion） |
| `spell3Inputs stack ctx getter1 getter2 getter3` | 3 | 三输入法术 |

#### 便捷构造器

| 函数 | 说明 |
|------|------|
| `makeConstant iota` | 返回一个 action，推入恒定值（如 `makeConstant Null`、`makeConstant (Boolean True)`） |

### 1.3 可用的类型 Getter

Getter 函数用于从栈上取出 Iota 并验证其类型。类型不匹配时自动返回 `Garbage IncorrectIota` 错误。

| Getter | 匹配的 Iota 构造函数 | 用途 |
|--------|---------------------|------|
| `getNumber` | `Number _` | 数值 |
| `getVector` | `Vector (_, _, _)` | 三维向量 |
| `getBoolean` | `Boolean _` | 布尔值 |
| `getEntity` | `Entity _` | 实体 |
| `getIotaList` | `IotaList _` | 列表 |
| `getInteger` | `Number n` 且 n 为整数 | 整数值 |
| `getPositiveInteger` | `Number n` 且 n ≥ 0 且为整数 | 非负整数 |
| `getNumberOrVector` | `Number _` 或 `Vector _` | 数值或向量 |
| `getNumberOrList` | `Number _` 或 `IotaList _` | 数值或列表 |
| `getIntegerOrList` | 整数或 `IotaList _` | 整数或列表 |
| `getPatternList` | 元素全为 `PatternIota` 的 `IotaList` | 图案列表 |
| `getPatternOrIotaList` | `PatternIota` 或 `IotaList` | 图案或列表 |
| `getPatternIota` | `PatternIota _ _` | 图案 |
| `getAny` | 任意 Iota | 通用（不做类型检查） |

### 1.4 完整示例：新增一个数学 Pattern

以新增一个 "求平均数" 的 Pattern 为例。

**第1步：在 `src/Logic/App/Patterns/Math.elm` 中编写 action 函数**

```elm
-- 求两个数的平均值
average : Array Iota -> CastingContext -> ActionResult
average stack ctx =
    let
        action iota1 iota2 _ =
            ( case (iota1, iota2) of
                (Number n1, Number n2) ->
                    Number ((n1 + n2) / 2)
                        |> Array.repeat 1

                _ ->
                    Garbage CatastrophicFailure
                        |> Array.repeat 1
            , ctx
            )
    in
    action2Inputs stack ctx getNumber getNumber action
```

**第2步：在 `src/Logic/App/Patterns/PatternRegistry.elm` 的 `patternRegistry` 列表中添加条目**

在文件末尾的 `]` 之前添加（约第559行）：

```elm
    , { signature = "awwdd"                                    -- 你的签名
      , internalName = "average"                               -- 内部名
      , action = average                                       -- action 函数
      , displayName = "Mean Distillation"                      -- 显示名
      , outputOptions = []                                     -- 无额外输出选项
      , selectedOutput = Nothing                               -- 无预选输出
      , startDirection = East                                  -- 起始方向
      }
```

**第3步：编译**

```bash
cd Hex-Studio-master
elm make src/Main.elm --output=gen/source.js
```

---

**新增 Pattern 后自动生效的功能：**

- ✅ 用户在网格上绘制匹配的六边形路径 → `getPatternFromSignature` 自动识别
- ✅ 用户在输入框中键入 `internalName` 或 `displayName` → `getPatternFromName` 自动匹配
- ✅ 通过文本导入语法解析 → `ImportParser.elm` 使用 `getPatternFromName`
- ✅ 项目保存/加载 → 签名会被序列化到项目文件中
- ✅ 颜色自动按照成功/失败着色（成功=accent1，失败=accent4）
- ✅ 图案在 Pattern Panel 中显示

### 1.5 新增 Great Spell（长签名图案）

Great Spell 与普通 Pattern 完全相同，只是注册位置不同。

在 `PatternRegistry.elm` 的 `greatSpellRegistry` 列表（第364-393行）中添加：

```elm
greatSpellRegistry : List Pattern
greatSpellRegistry =
    [ ...
    , { signature = "waawddqwea"
      , internalName = "my_great_spell"
      , action = myAction
      , displayName = "My Great Spell"
      , outputOptions = []
      , selectedOutput = Nothing
      , startDirection = East
      }
    ]
```

> **注意：** `greatSpellRegistry` 会自动通过 `List.map` 补全 `color`、`active`、`metaAction` 字段，然后与 `patternRegistry` 合并（第574行 `(++) greatSpellRegistry`）。

### 1.6 新增 Meta Pattern（UI 操作图案）

Meta Pattern 用于触发 UI 操作而非栈操作（如清除图案、重置等）。

在 `PatternRegistry.elm` 的 `metapatternRegistry` 列表（第578-598行）中添加：

```elm
metapatternRegistry : List Pattern
metapatternRegistry =
    [ ...
    , { signature = "aawwdd"
      , internalName = "my_meta_action"
      , action = noAction          -- 必须用 noAction
      , metaAction = ClearPatterns -- 选择元操作
      , displayName = "My Meta Action"
      , outputOptions = []
      , selectedOutput = Nothing
      }
    ]
```

可用的 `MetaActionMsg`：
- `None` — 无操作
- `ClearPatterns` — 清除所有图案
- `Reset` — 完全重置（图案+栈+上下文）
- `Backspace` — 删除上一个图案
- `Wrap` — 换行

---

## 二、新增 Iota 类型

**难度：★★★ 困难 | 侵入性：需修改 7 个文件 | 需要理解 Elm 联合类型的穷举匹配**

### 2.1 当前 Iota 类型一览

`Iota` 定义在 `src/Logic/App/Types.elm`（第80-90行）：

```elm
type Iota
    = Number Float                          -- 数值
    | Vector ( Float, Float, Float )       -- 三维向量
    | Boolean Bool                          -- 布尔
    | Entity String                        -- 实体（用字符串名称标识）
    | IotaList (Array Iota)                -- Iota 列表
    | PatternIota Pattern Bool             -- 图案引用（第二个 Bool 表示是否已被考虑）
    | Null                                 -- 空值
    | Garbage Mishap                       -- 垃圾/错误值（带错误信息）
    | OpenParenthesis (Array Iota)         -- 内省括号（用于 Consideration）
```

对应的类型标记（`IotaType`，第69-78行）：

```elm
type IotaType
    = NumberType
    | VectorType
    | BooleanType
    | EntityType
    | IotaListType IotaType
    | PatternType
    | NullType
    | GarbageType
```

### 2.2 必须修改的文件完整清单

由于 Elm 编译器对一个自定义联合类型的所有模式匹配进行**穷举检查（exhaustive check）**，任何新增的 variant 会导致所有 match 该类型的代码编译失败。以下文件**必须全部修改**：

| # | 文件 | 需要修改的内容 |
|:--:|------|--------------|
| 1 | `src/Logic/App/Types.elm` | ① 新增 Iota variant <br> ② 可能需要新增 Mishap <br> ③ 新增 IotaType（如有） |
| 2 | `src/Logic/App/Patterns/OperatorUtils.elm` | ① 新增对应的 getter 函数 <br> ② 更新 `checkEquality` 函数 <br> ③ 可能需要更新 `nanOrInfinityCheck` |
| 3 | `src/Logic/App/Stack/EvalStack.elm` | 更新 `introspection` 检测和 pattern matching 分支 |
| 4 | `src/Logic/App/ImportExport/ImportExportProject/ImportExportProjectV1.elm` | ① 新增 `SimplifiedIota` variant <br> ② `simplifyIota` 新增分支 <br> ③ `unSimplifyIota` 新增分支 <br> ④ `iotaCodec` 新增 variant |
| 5 | `src/Settings/Theme.elm` | `iotaColorMap` 函数新增分支 |
| 6 | `src/Logic/App/Utils/GetIotaValue.elm` | ① `getIotaValueAsString` 新增分支 <br> ② `getIotaValueAsHtmlMsg` 可能需要新增分支 <br> ③ `getIotaTypeAsString` 新增分支（如有新 IotaType）<br> ④ `getIotaFromString` 可能需要更新 <br> ⑤ `getIotaTypeFromString` 可能需要更新 |
| 7 | `src/Logic/App/Macros/UpdateMacroReferences.elm` | `updateIota` 函数新增分支 |

### 2.3 逐步操作指南

**第1步：修改 `Types.elm`**

在 `Iota` 类型中添加新 variant：

```elm
type Iota
    = Number Float
    | Vector ( Float, Float, Float )
    | Boolean Bool
    | Entity String
    | IotaList (Array Iota)
    | PatternIota Pattern Bool
    | Null
    | Garbage Mishap
    | OpenParenthesis (Array Iota)
    | NewType SomeData    -- ← 新增
```

如果需要新的类型标记，同时更新 `IotaType`。

**第2步：修改 `OperatorUtils.elm`**

新增 getter 函数：

```elm
getNewType : Iota -> Maybe Iota
getNewType iota =
    case iota of
        NewType _ ->
            Just iota

        _ ->
            Nothing
```

更新 `checkEquality`（第436行）:

```elm
checkEquality : Iota -> Iota -> Bool
checkEquality iota1 iota2 =
    ...
    case (iota1, iota2) of
        ...
        (NewType d1, NewType d2) ->
            d1 == d2    -- 根据实际数据类型调整

        _ ->
            iota1 == iota2
```

**第3步：修改 `EvalStack.elm`**

在第47-53行的 `introspection` 变量和接下来的 PatternIota 匹配之外，一般不需要特别处理（除非新 Iota 需要特殊的执行语义）。

**第4步：修改 `ImportExportProjectV1.elm`**

在 `SimplifiedIota` 类型中添加对应 variant：

```elm
type SimplifiedIota
    = ...
    | SimplifiedNewType SomeData    -- ← 新增
```

更新 `simplifyIota`：

```elm
simplifyIota iota =
    case iota of
        ...
        NewType data ->
            SimplifiedNewType data
```

更新 `unSimplifyIota`：

```elm
unSimplifyIota macros simplifiedIota =
    case simplifiedIota of
        ...
        SimplifiedNewType data ->
            NewType data
```

更新 `iotaCodec`（第257行）加入新的序列化 variant。

**第5步：修改 `Theme.elm`**

更新 `iotaColorMap`（第36行）：

```elm
iotaColorMap iota =
    case iota of
        ...
        NewType _ ->
            "#4C3541"    -- 选择合适的颜色
```

**第6步：修改 `GetIotaValue.elm`**

更新 `getIotaValueAsString`（第71行）：

```elm
getIotaValueAsString iota =
    case iota of
        ...
        NewType data ->
            "NewType: " ++ Debug.toString data
```

更新 `getIotaValueAsHtmlMsg`（第26行），一般可以与 `IotaList`/`OpenParenthesis` 之外的 `_ ->` 走相同的渲染路径。

如果有新 IotaType，更新 `getIotaTypeAsString`。

**第7步：修改 `UpdateMacroReferences.elm`**

更新 `updateIota` 函数（第62行）：

```elm
updateIota iota =
    case iota of
        ...
        NewType _ ->
            iota    -- 新类型通常不需要宏引用更新，直接返回自身
```

### 2.4 新增 Iota 后要检查的检查清单

- [ ] `Types.elm` — Iota + 可能的 IotaType + Mishap
- [ ] `OperatorUtils.elm` — 新增 getter + checkEquality 分支
- [ ] `EvalStack.elm` — 执行循环中的匹配
- [ ] `ImportExportProjectV1.elm` — 序列化/反序列化（5处修改）
- [ ] `Theme.elm` — iotaColorMap
- [ ] `GetIotaValue.elm` — 显示字符串转换（3-4处修改）
- [ ] `UpdateMacroReferences.elm` — updateIota
- [ ] 编译零错误：`elm make src/Main.elm --output=gen/source.js`
- [ ] 测试：新建项目 → 添加涉及该 Iota 的 Pattern → 导出导入 → 确认数据完整性

---

## 三、图案签名系统说明

图案的签名字符串由用户在六边形网格上绘制的路径自动生成。

### 字母-方向映射

签名使用 `q/w/e/a/d` 五个字母，每个字母代表一个 **连续两条线段之间的角度关系**：

| 字母 | 连续两段的方向组合 | 视觉示意 |
|:----:|-------------------|:--------:|
| `a` | 前一段东北 → 下一段西 | 左上转弯 |
| `w` | 前一段东南 → 下一段西 | 左下转弯 |
| `d` | 前一段西南 → 下一段东北 | 右下转弯 |
| `e` | 前一段西北 → 下一段东 | 右上转弯 |
| `q` | (特殊) 西向、回退等 | 后向/直线段 |

具体映射定义在：
- `src/Logic/App/Utils/LetterMap.elm` — 字母到方向对的映射
- `src/Logic/App/Utils/DirectionMap.elm` — 方向到坐标偏移的映射

### 签名生成流程

1. 用户在网格上绘制路径 → 得到一组 `GridPoint` 列表
2. `GetAngleSignature.elm` 对相邻点计算方向向量
3. 连续方向对通过 `letterMap` 转换成字母
4. 所有字母拼接成 `signature`，起始方向设为 `startDirection`

### 如何确定新 Pattern 的签名

**方法一（推荐）：** 在 Hex-Studio 的网格上直接绘制你想要的形状，观察生成的 Pattern 的 `signature` 值。

**方法二：** 手动推演
- 从起始点出发，按六边形网格坐标偏移确定每个方向
- 用 `directionMap` 查方向 → 坐标偏移
- 用 `letterMap` 查连续方向对 → 字母

**方向与坐标偏移关系（doubled coordinates）：**
```
Northeast → (1, -1)
East      → (2, 0)
Southeast → (1, 1)
Southwest → (-1, 1)
West      → (-2, 0)
Northwest → (-1, -1)
```

---

## 四、outputOptions 与 selectedOutput 机制

某些 Pattern 的输出依赖于外部状态（如是否命中实体、是否命中方块等），在模拟环境中无法确定输出值。`outputOptions` 和 `selectedOutput` 机制允许用户在 UI 中手动选择输出类型和值。

### 使用方式

```elm
{ signature = "wqaawdd"
, internalName = "raycast"
, action = raycast
, displayName = "Archer's Distillation"
, outputOptions = [VectorType, NullType]                      -- 两种可能的输出
, selectedOutput = Just (VectorType, Vector (0, 0, 0))        -- 默认选 Vector
, ...
}
```

- `outputOptions = []` → 输出确定，不需要用户选择
- `outputOptions = [VectorType, NullType]` → UI 会显示下拉选项让用户选择
- `selectedOutput` → 初始默认选中的值

### 在 Pattern Panel 中的表现

当 `outputOptions` 非空时，Pattern Panel 中该 Pattern 的右侧会出现一个下拉选择器，允许用户在模拟时覆盖输出值。这在以下场景中非常有用：

- `raycast`：击中返回 Vector，未击中返回 Null
- `get_entity`：找到返回 Entity，未找到返回 Null
- `zone_entity`：返回实体列表（可能为空）

---

## 附录：快速参考卡片

### 新增 Pattern 最小模板

```elm
-- ① 在 Patterns/*.elm 中
myAction : Array Iota -> CastingContext -> ActionResult
myAction stack ctx =
    let
        action iota1 iota2 _ =
            ( ... , ctx )
    in
    action2Inputs stack ctx getNumber getNumber action

-- ② 在 PatternRegistry.elm 的 patternRegistry 列表中
    , { signature = "XXXXX"
      , internalName = "my_id"
      , action = myAction
      , displayName = "My Display Name"
      , outputOptions = []
      , selectedOutput = Nothing
      , startDirection = East
      }
```

### 常用 action 模式

```elm
-- 推入常量
action = makeConstant (Number 42)

-- 无输入，仅修改上下文
action = actionNoInput stack ctx (\ctx -> (Array.empty, newCtx))

-- 单输入
action = action1Input stack ctx getNumber (\iota ctx ->
    case iota of
        Number n -> (Array.repeat 1 (Number (n * 2)), ctx)
        _ -> (Array.repeat 1 (Garbage CatastrophicFailure), ctx))

-- 双输入
action = action2Inputs stack ctx getNumber getVector (\iota1 iota2 ctx ->
    ...)

-- 法术（消耗输入但不产出新值到栈）
action = spell2Inputs stack ctx getVector getNumber
```
